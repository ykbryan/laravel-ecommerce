version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 10
        php: 7.3
  pre_build:
    commands:
      - echo Build started on `date`
  build:
   commands:
     - echo Installing composer deps
     - composer install
  post_build:
    commands:
      - echo Build completed on `date`